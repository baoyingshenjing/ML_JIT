REVIEW: Phase 1 produced solid code but has deviations from the reference paper's training recipe.
Fix ALL of the following issues. Read .ref/denoiser.py and .ref/main_jit.py for reference values.

ISSUES TO FIX:

1. **config.py defaults are WRONG** - must match paper exactly:
   - P_mean: -0.8 (not 0.0)
   - P_std: 0.8 (not 1.0)
   - t_eps: 5e-2 (not 1e-5)
   - noise_scale: 1.0 (for 256px) / 2.0 (for 512px) - config keeps 1.0 as default
   - ema_decay1: 0.9999 (not 0.999)
   - ema_decay2: 0.9996 (not 0.9999)
   - ADD --blr argument (base learning rate), default 5e-5. LR is computed as: lr = blr * batch_size / 256
   - epochs: 600 (not 400), warmup_epochs: 5 (not 20)
   - ADD --gen-bsz, --eval-freq, --online-eval arguments
   - label_drop_prob: keep 0.1 (correct)

2. **scripts/train.py** needs fixes:
   - Image normalization: the reference does NOT use torchvision Normalize. It does:
     x = x.to(float).div_(255) then x = x * 2.0 - 1.0 (in the training loop).
     So the transform should be: Resize + CenterCrop + RandomHorizontalFlip + ToTensor (PILToTensor is better, no normalization)
   - Add learning rate warmup scheduler (constant schedule with linear warmup)
   - Add online evaluation (generate images + compute FID) every eval_freq epochs using torch-fidelity
   - Use bf16 autocast (torch.amp.autocast('cuda', dtype=torch.bfloat16)) not fp16
   - Use AMP GradScaler or better: don't use scaler with bf16 (bf16 doesn't need loss scaling)
   - Add EMA saving to checkpoint (save both model params and EMA params)
   - Add seed for reproducibility
   - Add --blr and compute actual LR from blr * batch_size / 256

3. **denoiser.py** generation method:
   - The generate() function passes too many kwargs to stepper functions. The stepper functions
     (euler_step, heun_step) take (net, z, t, t_next, labels, num_classes, cfg_scale, t_eps, cfg_interval)
     which is correct. But verify the t shape handling matches reference.

4. **model.py** minor:
   - The dropout placement comment says "last quarter" but verify: dropout_start = depth - depth // 4
     For depth=12: 12-3=9, blocks 9,10,11 get dropout (3 blocks = last quarter). Correct.
   - Remove the `dynamic=True` from torch.compile in train.py

5. **scripts/generate.py**:
   - Add FID computation using torch-fidelity after generation, using the pre-computed
     fid_stats files from .ref/fid_stats/ (copy them into the project)
   - Add --evaluate flag

6. **pyproject.toml**:
   - Add [project.scripts] entry points:
     jit-train = "scripts.train:main"
     jit-generate = "scripts.generate:main"

CRITICAL: Fix the defaults and the training loop. The architecture (model.py, utils.py) is correct.
The training recipe must faithfully reproduce the paper's approach.
